/**
 * @param {number[]} prices
 * @return {number}
 */
// var maxProfit = function (prices) {
//   let res =
//     Math.max(
//       ...prices.slice(prices.findIndex((el) => el == Math.min(...prices)) + 1)
//     ) - Math.min(...prices);
//   return res > 0 ? res : 0;
// };

/**
 * @param {number[]} prices
 * @return {number}
 */
var maxProfit = function (prices) {};

console.log(maxProfit([2, 4, 1]));
